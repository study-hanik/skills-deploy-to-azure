name: Step 4, 스테이징에 배포하기

# 이 워크플로우는 step 4에서 step 5로 업데이트합니다.

# staging-test 브랜치에서 "Deploy to staging" 워크플로우가
# 완료된 후 실행됩니다
# 참고: https://docs.github.com/en/actions/learn-github-actions/events-that-trigger-workflows
on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Deploy to staging"]
    types: [completed]
    branches: [staging-test]

permissions:
  # Need `contents: read` to checkout the repository.
  # Need `contents: write` to update the step metadata.
  contents: write
  pull-requests: write

jobs:
  # Get the current step to only run the main job when the learner is on the same step.
  get_current_step:
    name: Check current step number
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - id: get_step
        run: |
          echo "current_step=$(cat ./.github/steps/-step.txt)" >> $GITHUB_OUTPUT
    outputs:
      current_step: ${{ steps.get_step.outputs.current_step }}

  on_azure_environment_created:
    name: Azure 환경 생성 확인
    needs: get_current_step
    runs-on: ubuntu-latest

    # We will only run this action when:
    # 1. This repository isn't the template repository.
    # 2. The step is currently 4.
    # Reference: https://docs.github.com/en/actions/learn-github-actions/contexts
    # Reference: https://docs.github.com/en/actions/learn-github-actions/expressions
    if: >-
      ${{ !github.event.repository.is_template
          && needs.get_current_step.outputs.current_step == 4 }}

    steps:
      # We'll need to check out the repository so that we can edit the README.
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Let's get all the branches.

      # 열린 풀 리퀘스트를 병합합니다
      - name: 풀 리퀘스트 병합
        run: gh pr merge --squash staging-test
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # main을 풀합니다.
      - name: main 풀
        run: |
          chmod +x ./.github/script/initialize-repository.sh
          ./.github/script/initialize-repository.sh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # README.md에서 step 4를 step 5로 전환합니다.
      - name: step 5로 업데이트
        uses: skills/action-update-step@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          from_step: 4
          to_step: 5
          branch_name: staging-test

      # Workshop Tracker: 단계 이벤트 전송
      - name: Workshop Tracker 이벤트 전송
        if: always()
        run: |
          curl -X POST -H "Content-Type: application/json" \
            -d '{"sessionId": "'"${{ secrets.WORKSHOP_SESSION_ID }}"'", "track": "deploy-to-azure", "eventType": "step", "step": 4, "repository": "'"${{ github.repository }}"'"}' \
            https://workshop-tracker-api.jollyocean-9c92d5de.koreacentral.azurecontainerapps.io/api/events || true
